<%= form_for logline do |f| %>
  <%= f.text_area :content, placeholder: 'Write your logline here...' %><br>
  <%= f.fields_for :categories, logline.categories.build do |categories_fields| %>
    	<%= categories_fields.text_field :name %><br>
  	<% end %>
  <%= f.submit %><br>
<% end %>
